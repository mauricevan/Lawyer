# Backend API (browser → backend on host port)
# docker-compose: http://localhost:8000 | docker-compose.local: http://localhost:8001
# Next.js reads env only at startup — restart dev server after changes:
#   npm run dev  OR  docker compose restart frontend
NEXT_PUBLIC_API_URL=http://localhost:8001
